程序运行需知：
1.服务器端需建立hydrogenMusicDB数据库
2.目前工作人员端的上传音乐（将.mp3解析出数据存到数据库）和一般听歌用户功能合并在了一个客户端上，所以这个客户端既可以以工作人员的角色上传音乐，又可以以一般用户的角色拥有听歌体验。
3.目前只能:任意选择一个.mp3文件上传至服务器,然后会在UI界面上显示，并可以下载到本地（其中包含了.mp3的传输），由于实现原因，若要播放，只能将文件手动放在hydrogenMusic/assets/music/目录下。
4.上次答辩，上传的爱的代价和动物世界，失败的原因：点击确认上传后，代码会解析.mp3里面的数据存入数据库，而刚好这两首歌里的数据不足，代码未对其作出若无某项数据就正常退出的异常处理，才会造成系统异常退出。


2019.04.29
运行此程序，出了龚老师的系统外，还要按转vlc-qt库和live555，安装过程参考官网，本次合并添加了在线播放的相关实现.
程序初次运行准备：
1、终端运行live555MediaServer，注意RTSP服务器的地址与客户端代码中的地址是否相同，不相同要修改客户端请求媒体资源的rtsp地址。
2、运行/root/tmp/mysql中的命令，然后创建相关数据库（数据库名参考代码）。
3、运行server、client


再次运行：
1、先启动数据库
2、live555
3、server client。


如果上传歌曲未传封面，则加载本地"../assets/advertise/0.jpg"，所以这个目录下这张图片不要删
